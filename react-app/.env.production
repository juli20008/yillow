# Production build values — safe to commit (no secrets here).
# Secret keys (REACT_APP_GOOGLE_MAPS_API_KEY) must be set in the
# Vercel dashboard under Settings → Environment Variables.

# Render backend URL (no trailing slash)
REACT_APP_API_URL=https://yillow-api.onrender.com
